// postcss.config.mjs
/* export default {
  plugins: {
    "@tailwindcss/postcss"({ env, parsedFile }) {
      // Only apply Tailwind+PostCSS to CSS files
      if (parsedFile && parsedFile.ext === ".css") {
        return {};
      }
      // Disable PostCSS for SCSS files
      return false;
    },
  },
};
 */
/* const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config; */